Cloner le projet
créer un fichier .env et y copier le contenu du fichier .env.example
lancer la commande : composer update
lancer la commande : php artisan storage:link
lancer la commande : php artisan migrate:install
lancer la commande : php artisan migrate
